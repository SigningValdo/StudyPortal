/**
 * Preuves de financement feature route definitions.
 * Consumed by AppRouter.tsx (lazy imports are defined there for code-splitting).
 */
export const PREUVES_ROUTES = {
  root: '/preuves-de-financement',
} as const;
